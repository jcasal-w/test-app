on:
  release:
    types: [created]
  
name: Release

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Validate
        uses: ./.github/workflows/validate.yml

      - name: Release
        run: echo "Releasing..."