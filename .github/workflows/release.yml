on:
  release:
    types: [created]
  
name: Release

jobs:
  validate:
    uses: ./.github/workflows/validate.yml
  release:
    needs: validate
    runs-on: ubuntu-latest
    steps:
      - name: Release
        run: echo "Releasing..."
